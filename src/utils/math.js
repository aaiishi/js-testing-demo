// src/utils/math.js

/**
 * Multiplie deux nombres.
 * @param {number} a - Le premier facteur.
 * @param {number} b - Le deuxième facteur.
 * @returns {number} Le produit de a et b.
 */
export function multiply(a, b) {
  return a * b;
}
